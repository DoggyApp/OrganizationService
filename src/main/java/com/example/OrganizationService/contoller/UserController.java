package com.example.OrganizationService.contoller;

// All user auth endpoints (login, session, logout, password, getAll, getById)
// have been moved to the UserService (port 8082).
// User management (create/delete) is handled by OrganizationController.
public class UserController { }
